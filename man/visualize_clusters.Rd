% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_clusters.R
\name{visualize_clusters}
\alias{visualize_clusters}
\title{Visualize Clustering Results with t-SNE and UMAP}
\usage{
visualize_clusters(
  x,
  pam_fit,
  df,
  id_col,
  umap_nneight = c(10L, 15L, 20L, 25L, 30L, 35L, 40L, 45L, 50L),
  umap_min_dist = c(0.1, 0.25, 0.3, 0.5, 0.75, 0.99),
  point_size = 2,
  stability_col = "stability",
  save_plots = FALSE,
  plot_outpath = "."
)
}
\arguments{
\item{x}{A dissimilarity matrix (must be of class "dissimilarity").}

\item{pam_fit}{A fitted PAM object (e.g., from \code{cluster::pam()}).}

\item{df}{Data frame containing metadata including IDs and stability scores.}

\item{id_col}{Character. Name of the ID column in \code{df}.}

\item{umap_nneight}{Integer vector. Values for the UMAP \code{n_neighbors} parameter.}

\item{umap_min_dist}{Numeric vector. Values for the UMAP \code{min_dist} parameter.}

\item{point_size}{Numeric. Size of points in the plots. Default is 2.}

\item{stability_col}{Character. Column name in \code{df} that stores cluster stability metrics.}

\item{save_plots}{Logical. If \code{TRUE}, plots are saved to \code{plot_outpath}.}

\item{plot_outpath}{Character. Directory path for saving plots. Default is current directory.}
}
\value{
A list with:
\describe{
\item{tsne_plot}{A ggplot2 object visualizing clusters via t-SNE.}
\item{umap_plots}{A nested list of ggplot2 UMAP plots indexed by neighbor and distance parameters.}
}
}
\description{
Visualizes cluster assignments from a PAM model using t-SNE and UMAP dimensionality
reduction techniques. UMAP is computed for several combinations of \code{n_neighbors}
and \code{min_dist} values. The function returns plots and optionally saves them.
}
